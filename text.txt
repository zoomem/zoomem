#include <string>
#include <fstream>
#include <streambuf>
#include <regex>
#include <iostream>
#include <iterator>
using namespace std;
int foo(int x)
{
	return 0;
}
int main()
{
	regex e ("\\b(sub)([^ ]*)"); 
	
	ifstream t("text.txt");
	string str((istreambuf_iterator<char>(t)),istreambuf_iterator<char>());
	cout << str << endl;
	return 0;
}

